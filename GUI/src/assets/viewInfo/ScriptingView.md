# Scripting View

Use the scripting view to script *module* parameters using the [Lua programming language](http://lua.org)

To add a script to a *block*: select block -> scripting tab in properties panel -> ‘+’

